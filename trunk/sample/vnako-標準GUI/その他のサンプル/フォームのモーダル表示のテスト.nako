#-----------------------------------------------------------------------
# モーダル表示の利用例
#-----------------------------------------------------------------------
子フォームとはフォーム。
そのテキストは「子フォーム」。

ＯＫボタンとはボタン
その親部品は子フォーム。
そのＸ＝１０
そのＹ＝１０
これをクリックした時は
　　子フォームを閉じる。

子フォームをモーダル表示
