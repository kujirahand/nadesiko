#-----------------------------------------------------------------------
# カスタム関連付けの例
#-----------------------------------------------------------------------
パスはナデシコランタイムパス
#---
「".test"という拡張子に、
"{パス}"を関連付けする例です。
実行してよいですか？」と二択。
もし、それがいいえならば、終わり。

# 関連付けの例
「HKEY_CLASSES_ROOT\.test」の「」に「testFile」をレジストリ値設定。
「HKEY_CLASSES_ROOT\testFile\shell\open\command」の「」に「"{パス}" "%1"」をレジストリ値設定。

「終わりました。」と言う。
終わり。
