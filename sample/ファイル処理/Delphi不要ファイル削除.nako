「Thumbs.db」の不要削除処理。
「*.~*」の不要削除処理。
「*.dcu」の不要削除処理。
「*.ddp」の不要削除処理。
「*.exe」の不要削除処理。
「おわり」と、言う。
おわり。

●不要削除処理（WCの）
    母艦パス＆WCの、全ファイル列挙して、一覧に代入。
    もし、一覧が空ならば戻る。
    「以下を削除：｛改行｝｛一覧}」とメモ記入。
    もし、それが空ならば、戻る。
    一覧を反復
		母艦＝それ
		対象をファイル削除。
