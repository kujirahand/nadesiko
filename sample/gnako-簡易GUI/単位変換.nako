# 単位変換プログラム

移動( 10, 10)。「入力：」と表示
移動( 50, 10)。元エディタとはエディタ
移動(200, 10)。「メートル」と表示

移動( 10, 50)。「結果：」と表示
移動( 50, 50)。結果エディタとはエディタ
移動(200, 50)。

単位リストとはリスト
単位リストについて
　アイテムは「メートル｛～｝インチ｛～｝尺｛～｝東京タワー」
　単位リストの値は０

移動(10, 180)。

計算ボタンとはボタン
計算ボタンのクリックした時は～
　結果とは数値
  元は、元エディタのテキスト
　単位リストのテキストで条件分岐
  　「メートル」ならば、結果は元
    「インチ」ならば、結果は(元÷0.0254)
    「尺」ならば、結果は(元÷0.30303)
    「東京タワー」ならば、結果は元÷333
  結果エディタのテキストは、結果

元エディタの変更した時は～
　　計算ボタンのクリックした時。
