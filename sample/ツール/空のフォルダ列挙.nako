＃デスクトップ＆「Laputa共有」の全ファイル列挙して言う
＃おわり
フォルダ選択して対象フォルダへ代入。
もし、対象フォルダが空ならば終わり。
#-----------------------------------------------------------------------
# 空のフォルダ列挙
#-----------------------------------------------------------------------
対象フォルダを再帰検索処理。
表示ログを言う。

●再帰検索処理（対象を）
　　母艦は対象
　　対象のフォルダ列挙して反復
    　　「{対象}{それ}\」を再帰検索処理
    対象の全ファイル列挙
    もし、それが空ならば
    　　＃対象のフォルダ削除。
        対象を表示。
