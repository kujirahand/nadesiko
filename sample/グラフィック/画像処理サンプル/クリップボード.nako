# クリップボードの画像を操作

仮想画面とはイメージ。
仮想画面の可視はオフ。

表示画面とはイメージ
そのＹは100

クジラコピーボタンとはボタン
そのＹは１０
そのＷは100
これをクリックした時は
　　仮想画面の画像は「{ランタイムパス}nadesiko.png」
    仮想画面を「クリップボード」に画像保存。

ワニコピーボタンとはボタン
そのＹは40
そのＷは100
これをクリックした時は
　　仮想画面の画像は「{ランタイムパス}lib\wani.gif」
    仮想画面を「クリップボード」に画像保存。

画像貼り付けボタンとはボタン
そのＹは70
そのＷは100
これをクリックした時は
　　表示画面の画像は「クリップボード」
    描画処理反映


