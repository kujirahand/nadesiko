#-----------------------------------------------------------------------
# HTTP簡易ポストの例
# -- ファイルを添付する場合
#-----------------------------------------------------------------------
# -- 必要ならBASIC認証の設定
HTTPオプション＝「BASIC認証=オン
ID=hoge
パスワード=hoge」
# -- ファイルを添付する
値＝空
値＠「userfile」=「@file={ランタイムパス}history.txt」
# -- ポスト(test)
「http://localhost/tools/etc/page/test/attach/write」へ,
値をHTTP簡易ポスト
# --- 結果を得る
ＳＪＩＳ変換
それをメモ記入。
終わる。
