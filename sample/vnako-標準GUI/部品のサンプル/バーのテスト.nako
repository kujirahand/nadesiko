# バーのテスト
母艦は「バーのテスト」
文字サイズは32

「バーを変えてみてください。
★0～50」と表示。
値バーとはバー。
そのXは10
そのYは100
そのWは250
そのHは48

値エディタとはエディタ。
そのXは300
そのYは100
そのWは200
そのHは48
その文字サイズは文字サイズ

値バーの最大値は、50
値バーの最小値は、０
値バーの変更した時は
　　値エディタのテキストは値バー。

値エディタのキータイピング時は
    もし、値エディタの押されたキー＝「{\13}」ならば
    　　値バーは、値エディタ
