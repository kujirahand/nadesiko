#-----------------------------------------------------------------------
# データベース手順
#-----------------------------------------------------------------------
# 実行には ACCESS2000以上 が必要です。
「データベース(ACCESS DB) のＳＱＬを使う例です。」と表示。

# データベースを開く
母艦パス&「test.mdb」をACCESS開く。

# 「顧客」テーブルで「%くん」を含むものを検索
「SELECT * FROM 顧客 WHERE 名前 LIKE "%くん"」をＳＱＬ実行
# 検索結果を全部一度に取得
ＤＢ結果全部取得して表示

# 閉じる
ＤＢ閉じる。

